#!/usr/bin/env bash
set -euo pipefail

# Dev run script — run the macOS app as a debug build
# Usage: ./scripts/run-dev.sh

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "==> run-dev"
echo "    (work in progress: replaced by xcodebuild + run once the app skeleton is done)"
echo "    note: 'real verification' requires on-device ADB logs and DeX screen confirmation (AGENTS.md)"

exit 0
