import Foundation

/// CGEventTap-based input capture (skeleton)
/// Hard rule: never trap the pointer; suppression requires timeout + fail-safe.
public final class InputCapture {
    public init() {}
}
